// const img = document.querySelector(".web3")
 

// function updateImageSource(){
//    // If the viewport width is greater than 1024px (desktop)
//    if (window.innerWidth >= 1024) {
//     img.src = 'image-web-3-desktop.jpg';  // Change to desktop image
// } else {
//     img.src = 'image-web-3-mobile.jpg';   // Change to mobile image
// }
// }
  
// // Call the function initially
// updateImageSource();

// // Add an event listener to update image when resizing the window
// window.addEventListener('resize', updateImageSource);  